#pragma once

#include "Keyboard.h"
#include "Mouse.h"

#include "key_config.h"
#include "keymap.h"

const int row_count = MATRIX_ROWS;
const int col_count = MATRIX_ROWS;

int row_pins[MATRIX_ROWS] = MATRIX_ROWS_PINS;
int col_pins[MATRIX_COLS] = MATRIX_ROWS_PINS;

void setup() {}

void loop() {}
